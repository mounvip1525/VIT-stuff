style={{
            backgroundColor: draggedItem === index ? 'lightblue' : 'white',
            margin: '20px 25%',
            textAlign: 'center',
            fontSize: '40px',
            border:
              draggedItem === index
                ? '2px dashed blue'
                : '2px solid black',
            cursor: 'move',
          }}
